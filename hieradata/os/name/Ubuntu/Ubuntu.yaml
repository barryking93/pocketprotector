# hieradata/os/name/Ubuntu/Ubuntu.yaml

pocketprotector::gpu::nvidia::package: cuda

pocketprotector::packages:
  - pciutils

pocketprotector::puppet::packages::client:
  - puppet-agent
  - puppetdb-termini

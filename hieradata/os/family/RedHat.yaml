# hieradata/os/family/RedHat.yaml

pocketprotector::packages::provider: dnf

pocketprotector::puppet::client::packages:
  puppet-agent:
    ensure: latest
pocketprotector::puppet::server::packages:
  puppetserver:
    ensure: latest

pocketprotector::puppet::server::defaults: /etc/sysconfig/puppetserver

pocketprotector::security::password::enforcer: pwquality


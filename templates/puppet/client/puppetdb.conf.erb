# templates/puppet/client/puppetdb.conf.erb
# <%= scope().call_function('hiera',['pocketprotector::managed']) %>
#

[main]
server_urls = https://<%= scope().call_function('hiera',['pocketprotector::puppetdb::server']) %>:8081

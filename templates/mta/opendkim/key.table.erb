<% scope().call_function('hiera',['pocketprotector::mta::opendkim::domains']).each do |opendkimdomain, hash| -%>
<%= scope().call_function('lookup',['pocketprotector::mta::opendkim::domains.' + opendkimdomain + '.selector',String,'deep']) %>._domainkey.<%= scope().call_function('lookup',['pocketprotector::mta::opendkim::domains.' + opendkimdomain + '.fqdn',String,'deep']) %>     <% scope().call_function('lookup',['pocketprotector::mta::opendkim::domains.' + opendkimdomain + '.fqdn',String,'deep']) %>:<%= scope().call_function('lookup',['pocketprotector::mta::opendkim::domains.' + opendkimdomain + '.selector',String,'deep']) %>:/etc/opendkim/keys/<%= opendkimdomain %>.private
<% end %>
